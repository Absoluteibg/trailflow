apiVersion: v1
kind: Service
metadata:
  name: trailflow
  labels:
    app: trailflow
spec:
  selector:
    app: trailflow
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  type: ClusterIP
